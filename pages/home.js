const React = require('react');
const HomePage = require('../components/home-page');

const Home = () => {
    return (
        <div>
            <HomePage />
        </div>
    );
};

module.exports = Home;